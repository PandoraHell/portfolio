/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
      remotePatterns: [
        {
          protocol: 'https',
          hostname: 'qafvsoqvpyonbmszyyve.supabase.co', 
        },
      ],
    },
  };
  
  export default nextConfig; 